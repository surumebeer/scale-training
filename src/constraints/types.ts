export type KeyName = {
  id: number;
  name: string;
};

export type ScaleName = {
  id: number;
  name: string;
};

export type SubScaleName = {
  id: number;
  name: string;
  degrees: number[];
};
